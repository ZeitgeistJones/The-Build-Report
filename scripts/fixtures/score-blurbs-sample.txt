# OPTIONAL offline fallback only.
# Prefer:  vercel env pull .env.local   then   npm run scan:dictionary
# That pulls live rubric blurbs from production — no pasting.
#
# Use this file only with:  npm run scan:dictionary:offline
